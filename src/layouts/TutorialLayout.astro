---
const { title, description, image, updated } = Astro.props;
---

<Layout>
  <main class="max-w-4xl mx-auto px-6 py-16 space-y-8 text-gray-800">
    <header class="text-center space-y-4">
      <img src={image} alt={title} class="mx-auto w-40 h-40 rounded-xl shadow-md" />
      <h1 class="text-4xl font-bold text-primary">{title}</h1>
      <p class="text-gray-600">{description}</p>
      <p class="text-sm text-gray-500">🗓 Actualizado: {updated}</p>
    </header>

    <article class="prose prose-lg max-w-none dark:prose-invert">
      <slot />
    </article>
  </main>
</Layout>
